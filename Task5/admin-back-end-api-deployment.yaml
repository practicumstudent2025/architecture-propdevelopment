apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin-back-end-api-app
  namespace: task5
  labels:
    role: admin-back-end-api
spec:
  replicas: 1
  selector:
    matchLabels:
      role: admin-back-end-api
  template:
    metadata:
      labels:
        role: admin-back-end-api
    spec:
      containers:
      - name: nginx
        image: nginx
        ports:
        - containerPort: 80
---
apiVersion: v1
kind: Service
metadata:
  name: admin-back-end-api-app
  namespace: task5
spec:
  selector:
    role: admin-back-end-api
  ports:
  - port: 80
    targetPort: 80
